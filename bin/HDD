#!/usr/bin/env bash

if [ "$UID" -ne 0 ]; then 
	echo Error: permission denied.
	exit 1
fi

if lsblk -f | grep HDD | grep -q /mnt/HDD; then 
	umount /mnt/HDD
	echo Unmounting HDD.
else 
	mount UUID=9dd86e7a-4995-414c-906f-489ca8cc1c3b /mnt/HDD/
	echo Mounting HDD.
fi
